---
name: cli
description: Run a mydocs CLI command. Usage /cli <command> [args].
argument-hint: <command> [args]
allowed-tools: Bash
---

# CLI

Run the mydocs CLI command:

```bash
uv run mydocs $ARGUMENTS
```

After running, report the output and any errors. If the command fails, suggest fixes.
